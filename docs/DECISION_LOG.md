# Bookmarkt Decision Log

This log records accepted material product and governance decisions. Detailed
implementation choices may use separate Architecture Decision Records later.

| ID | Date | Status | Decision | Consequence |
| --- | --- | --- | --- | --- |
| D-001 | 2026-08-16 | Accepted | Bookmarkt uses a nine-stage process: Stabilization, Architecture, UI/UX, Monetization, Native Packaging, Compliance, Beta, Launch, and Operations/Growth. | Work and approval follow the stage order defined in the roadmap. |
| D-002 | 2026-08-16 | Accepted | Repository roadmap, governance, gate records, and decision history are the durable source of truth. | Conversation and local task history remain supporting context only. |
| D-003 | 2026-08-16 | Superseded | Stage 2 creates an app-quality shared web/PWA core; it does not abandon the PWA. Native iOS/Android packaging occurs in Stage 5. | Superseded by D-008 because the final product is native-app-only. |
| D-004 | 2026-08-16 | Accepted | Supabase Pro is not required to enter Stages 2-6. Daily backups and leaked-password protection are mandatory before Stage 7 external beta. | Stage 1 can close on stability evidence; external users cannot be invited until the paid controls are active. Upgrade earlier if data value or Free limits justify it. |
| D-005 | 2026-08-16 | Accepted | AI and spoiler reports require human review before corrective action. Raw reports do not automatically modify user content or retrain a model. | Stage 2 adds report lifecycle foundations, Stage 6 adds secure administration and operations, and Stage 7 requires active monitoring. |
| D-006 | 2026-08-16 | Accepted | The Stage 1 stability window runs for seven consecutive 24-hour periods after all production-phone checks passed at 10:55 MDT (16:55 UTC). | Earliest Stage 1 exit review is August 23, 2026 at 10:55 MDT (16:55 UTC), assuming no P0/P1 defect. |
| D-007 | 2026-08-16 | Superseded | Printed QR codes must use a Bookmarkt-controlled durable HTTPS destination with native deep-link behavior and web fallback. | Superseded by D-008. The durable destination remains, but its fallback routes mobile users to the correct app store rather than the reading PWA. |
| D-008 | 2026-08-16 | Accepted | Bookmarkt v1 is a native iOS/Android product. A physical QR opens the installed app or routes an uninstalled mobile user to the Apple App Store/Google Play. The current PWA is a temporary prototype and is retired as a public reading product at Stage 8. | Stage 2 builds the native app core; Stage 5 completes app/store routing and native integration; Stage 8 launches the apps and retires the PWA. Minimal web infrastructure remains only for smart links, installation guidance, privacy, support, and account obligations. Supersedes D-003 and D-007. |
| D-009 | 2026-08-16 | Superseded | Bookmarkt v1 limits AI to three paid capabilities: AI text summary, AI character mapping, and AI image generation. The backend must verify the authenticated user's active paid tier, requested feature entitlement, and quota before any provider call. | Superseded by D-010, which preserves these controls and fixes the exact cumulative tier assignments. |
| D-010 | 2026-08-16 | Superseded | Bookmarkt has three cumulative paid tiers: Base includes AI Summary only; Base+ includes AI Summary and AI Character Mapping; Ultimate includes all Base+ features plus AI Image Generation. When no paid subscription is active, the app displays all three tiers. A verified purchase enters the paid stream; no selection or a canceled/failed/abandoned purchase returns to manual entry. | Superseded by D-011, which preserves the fixed tier ladder and defines simultaneous multi-feature generation. |
| D-011 | 2026-08-16 | Superseded | Paid tiers authorize feature sets in one generation session, not an either/or workflow. Base generates AI Summary. Base+ may generate Summary, Character Mapping, or both together. Ultimate may generate any one, any two, or all three entitled features together. The user controls amount/detail per selected feature within tier limits. | Superseded by D-012, which removes AI-generated content from the product entirely and replaces the tier ladder with a single AI Reading Companion subscription. The server-authoritative entitlement rigor defined here carries forward. Supersedes D-010. |
| D-048 | 2026-09-02 | Accepted | **The companion chat ships (Stage 4 Phase 2, first surface).** A book-scoped conversation screen (`/companion`) reached from a "Talk it over with the Companion" row on the book screen. The mascot speaks in the fixed D-038 rule-set (calm, non-judgmental, deadpan-scholarly, no emoji) with archetype flavor from logged genres; every reply carries a visible provenance chip ("From your notes" / "From my knowledge" / "Your notes + my knowledge", D-039) and spoiler-guarded declines render as a gold "Spoiler held back" chip. A boundary chip in the context bar states the current guard ("Nothing past page N"). First-visit intro offers three suggestion chips to lower the first-message hurdle. Unentitled readers see the subscription-offer card (no purchase flow until Phase 3 billing); daily-limit denials show the server's friendly copy. Conversation history persists per book (`companion_messages`) across sittings and devices. New analytics: `companion_opened`, `companion_message_sent` (status only — never content). | The competitor-familiar chat pattern (Fable's AI features, standard messaging UI) in the D-040 clean design: paper surfaces, terracotta accent, serif speaker label, gold reserved for the premium lock and spoiler-guard markers. 213 tests across 18 suites pass; shipped OTA. |
| D-047 | 2026-09-02 | Accepted | **The companion server foundation ships (Stage 4 Phase 1).** (1) `companion_entitlements` is the server-authoritative subscription record (statuses none/comped/trial/active/expired/canceled; sources none/dev_comp/trial/app_store/play_store) with RLS letting a reader see only their own row and only the service role write; every pre-existing account is comped via `dev_comp` for development testing (revocable before external beta). (2) A new `companion` Edge Function is the single gatekeeper for all AI requests: authenticated user → active entitlement → per-feature daily quota (`consume_companion_quota` RPC with per-user and project-wide caps under advisory locks) **before** any provider call; a denial returns a clear subscription-offer response (402) or friendly quota copy (429) and costs nothing. (3) `companion_usage_events` audits every request (entitlement decision, feature, quota outcome, model, token counts, latency, grounding source counts) without storing entry text. (4) Context assembly runs under the caller's own JWT so RLS bounds what the companion can read; replies persist to `companion_messages` (immutable, owner-scoped, topic-ownership-checked inserts per the D-041 pattern). (5) The client entitlement gate is rewritten to mirror the server decision for rendering only (`resolveCompanionEntitlement`), replacing the Stage 2 hard-off flag; `topics.genre` now captures the Google Books category on add to feed the D-038 archetype profile. Provider: Gemini 2.5 Flash, same key as the dormant legacy function. | Phase 1 of [STAGE_4_BUILD_PLAN.md](STAGE_4_BUILD_PLAN.md) is complete; the migration and function are live on the linked project. No user-visible change ships in this round. 207 tests across 17 suites pass. |
| D-046 | 2026-09-02 | Accepted | **Stage 3 exits with `GO`; Stage 4 (Monetization and accounts) is entered.** The owner closed the dogfooding log with every logged friction point and defect fixed (no open P0/P1), verified small-screen touch and character-map interactions on the physical device, passed the full 8-test manual accessibility audit with zero defects (2026-09-01), scan-verified a printed physical bookmark sample, and approved the v1 experience and brand direction. Four deferrals are named and destination-gated in [gates/STAGE_3_EXIT.md](gates/STAGE_3_EXIT.md): moderated usability tests with representative readers (plus finding resolution and funnel verification) move to **Stage 6** so real readers test the complete feature set; store-to-first-run onboarding moves to **Stage 5/6** store presence; companion provenance labels ship inside the **Stage 4** companion build; tablet/orientation/text-scaling layouts ride the **Stage 5** platform pass. | Owner 2026-09-02: "Dogfooding log completed... Small screen touch checks completed... I will have users try this when the features are complete. Deferred. ... I tried the QR code and it works." Stage 4 work may begin; approval tag `stage-3-approved`. 200 tests across 17 suites green at exit. |
| D-045 | 2026-09-02 | Accepted | **Character capture goes name-first and position-aware, with inline @mentions.** (1) The add-character form saves on a name alone (Add button beside the field); role/notes/relationships hide behind an "Add details" toggle (speak mode still opens them for dictation). (2) Suggestion chips mine likely character names from the reader's own entries (capitalized-run heuristic with stopword, lowercase-elsewhere, and sentence-start filters; unmatched @mentions rank first); one tap adds the character. (3) Every add stamps the reader's current position as a "First noted: page N" line in the shared PWA-compatible description encoding; cards show "First noted · Page N" and the list sorts by order of appearance (unstamped cards keep created order, after stamped ones). (4) Typing "@" in the entry composer suggests character names as one-tap chips; saved entries render matched @mentions as tappable links that jump to (and briefly highlight) the character's card - the linkable-card groundwork for the post-beta visual map. Analytics gains a `via` prop (form/quick/suggestion) on `character_map_saved`. | Owner 2026-09-02: implement the high-impact capture combo alongside inline "@" mentioning, and start the groundwork by making character cards linkable. Mentions are plain text in the entry - no schema change; the PWA ignores the First-noted line (it is retired per D-008). 200 tests across 17 suites pass. |
| D-044 | 2026-09-02 | Accepted | **The cover picker gains an enlarged preview and swap-aware fills.** (1) Tapping the selected cover opens a full-screen enlarged view (Google covers render their medium rendition, with fallback if unavailable; tap anywhere closes). (2) Switching between cover candidates now updates author and pages along with the cover: a field is swappable when it is blank **or still holds the value a previous pick supplied** - typed or stored input always wins (the D-033 contract), and the one-tap Undo now also restores the swap-tracking state. The logic lives in a pure, unit-tested `coverFillUpdates` helper shared by the add and edit forms. | Owner 2026-09-02: tapping the selected cover should show an enlarged image, and swapping covers should update author and pages for each selected cover. 176 tests across 15 suites pass. |
| D-043 | 2026-09-02 | Accepted | **The cover picker searches Google Books first**, with Open Library as the silent fallback - the same source order as search and scan, so a manual entry's "Find covers" returns the covers a search would have found. Candidates dedupe by cover URL; a picked candidate still fills blank author/pages with one-tap Undo (D-033). Attribution reads "Covers from Google Books and Open Library". The exact-edition ISBN path in the picker is unchanged (Open Library edition lookup). Also fixed the same day: page lookups retry title-only when Google's author-filtered query returns a pageCount-0 edition (seen live with SAO Progressive 1). | Owner 2026-09-02: manual entries' cover search "looks for open library and not google - it should be google." Open Library's cover set skewed older editions; Google's covers match what the search results already show. 169 tests across 15 suites pass. |
| D-042 | 2026-09-02 | Accepted | **Adding a book is search-first, powered by Google Books.** The add-book screen leads with a single search field (title, author, or a pasted ISBN) showing debounced live results - cover thumbnail, title, author, year, pages - and one tap on a result adds the book and returns to the library. The barcode scan sits beside the search field and now adds the matched book immediately (scanning is the intent to add); manual entry survives behind a "Can't find it? Add it manually" toggle, and a scan miss opens that form with the ISBN kept. **Google Books is the primary metadata source** for search and scan: best-in-class title/author search on a free public endpoint, and the general Google APIs terms permit commercial apps - Bookmarkt never charges for book data, only for the AI companion. This reverses the D-028 caution that declined Google Books (that reading applied the retired Books-specific terms). **Open Library remains** the cover-picker source (many edition covers per work) and the silent fallback whenever Google is unreachable or rate-limited; attribution for both renders in the form. ISBNdb was evaluated and declined (paid tiers ~$15-50/month; weaker title search than Google). The competitor set that grounds this and future UX decisions is documented in [COMPETITOR_REFERENCES.md](COMPETITOR_REFERENCES.md). | Owner 2026-09-02: "Adding books is not an easy process" - evaluate the competition and bring their add flow into the app; owner chose Google Books over ISBNdb. Search-first one-tap add with scan adjacent and manual as fallback is the shipped pattern in every reference app (Goodreads, StoryGraph, Fable, Bookmory, Bookly). New `bookSearch.ts` module with pure, unit-tested parsers; 151 tests across 15 suites pass. |
| D-041 | 2026-09-01 | Accepted | **Content writes now require topic ownership, verified by a simulated-user isolation probe.** A live cross-account check found that while reads, updates, deletes, and `user_id` spoofing were correctly blocked, the insert policies on `entries`, `characters`, and `book_images` did not verify the caller owns the referenced book (topic) - a user could attach self-owned rows to another user's book ID (no data was readable; the flaw was write-side only). Migration `20260901190000_require_topic_ownership_on_content_writes.sql` tightens those insert/update policies to require `topics.user_id = auth.uid()` and is applied to the live project and recorded in migration history. Isolation is now verified by `scripts/rls-isolation-check.mjs`, which simulates two authenticated users at the SQL layer against the real live policies (no real accounts, no email signups) and runs 13 checks covering reads, writes, spoofing, and cross-topic attachment - all passing. | The Stage 2 carried item "safe Supabase test project + explicit cross-account isolation tests" closes via this safer, cheaper route; a dedicated paid test project is deferred until Stage 7 beta scale. The probe script is rerunnable before any release. Scripted email-based test signups are not viable (GoTrue rejects reserved domains and rate-limits confirmations), which is why simulation at the SQL layer is the standing approach. |
| D-040 | 2026-09-01 | Accepted | The MVP interface moves from the skeuomorphic bookshelf to a **clean, competitor-matched design** in a warm neutral palette: paper-white surfaces, warm ink text, a single terracotta accent, flat cover-first 3-across library grid with "Currently reading" and "Finished" sections, reading-stats chips, a continue-reading hero card, and **bottom-tab navigation** (Library, My bookmarks, Settings) matching the pattern of StoryGraph, Goodreads, Fable, Bookmory, and Kindle. Serif typography stays on headers and book titles as the literary identity; gold is reserved for finished-book and celebration markers; wooden shelves, leather, and paper textures are retired. Offline-aware error states (friendly "you're offline" messaging with retry) ship as part of the same pass. | Owner decision 2026-09-01: the beta must look professional and familiar to readers of existing book apps, while the full mascot-era redesign (D-038) is professionally outsourced only after closed-beta validation proves buy-in. Supersedes the bookshelf visual direction refined in D-023-D-037 (those rows stand as history). The library, navigation, theme tokens, and book cards were rebuilt; all 139 tests across 14 suites pass. |
| D-039 | 2026-09-01 | Accepted | The **companion feature set is revised** around the mascot (D-038): (1) the **vocabulary bank becomes the mascot's word bank** - AI-generated to the reader's level (a light assessment on first premium use) and dominant genres (a cyberpunk reader and a Shakespeare reader see different banks), plus manually added words; presented as a box the mascot keeps, pulling out a word with definition, pronunciation, and example sentence; (2) **Quote Logs** are added - readers save favorite quotes from their books (free capture), and on the paid tier quotes feed companion personalization; (3) the **capture structuring aid** becomes a planned premium feature, superseding the on-hold D-027 proposal: the mascot asks leading questions and helps the reader arrange their own words into a structured summary or character record - the AI never generates or summarizes content for the reader, who remains the author of every saved word (this amends D-012/D-016 only to permit question-asking and arrangement assistance, never authorship); (4) **semantic search is kept**, with premium onboarding required to clearly explain what it does; (5) **cross-book threads become background mascot reasoning** - the clustering runs behind the scenes to power the mascot's observations and recommendations rather than as a standalone surface (final naming subject to marketing); (6) the **continuity flag is removed**, replaced by the **important event flag** - readers mark events that matter (free, manual), and the paid tier can auto-suggest them from the reader's entries; (7) **pattern recognition** ("I noticed you highlight ideas about psychology...") is recorded as a **key differentiator on the backburner** - it makes each reader's mascot experience unique, but it is the heaviest engineering item (embeddings + clustering over all entries) and is deliberately sequenced after closed-beta buy-in. | Owner decision 2026-09-01 from the proposed-modifications document. AI-generated reference material (word definitions, examples) does not breach the D-012 content boundary, which protects the reader's own records - the reader's summaries, notes, and maps are never AI-written. Book-club prep, recaps, Socratic dialogue, cue cards, and quizzes continue unchanged. Stage 4 builds the revised set; pattern recognition stays on the backlog with differentiator status. |
| D-038 | 2026-09-01 | Accepted | Bookmarkt's paid companion is embodied by a **mascot: a distinguished scholarly capybara** (tweed jacket, sage waistcoat, bow tie, monocle, leather journal, fountain pen - adult, dignified proportions, never chibi/cartoon), who delivers every premium feature. His **personality is dynamic**: a spider chart of intellectual archetypes (The Analyst - thrillers/mystery; The Empath - romance/drama; The Philosopher - non-fiction/classics; The World-Builder - fantasy/sci-fi) is silently updated from the reader's logged genres and alters the system prompt governing his conversational flavor. Humor comes from **deadpan contrast** - the visuals never change, only the dialogue leans into the dominant archetype (a romance-heavy chart gets a Victorian sociologist analyzing love triangles, not a swooning cartoon). He is strictly non-judgmental (no scolding or grading, per the zero-guilt philosophy) and book-genre-neutral in appearance. Professional art is specced (5 layered vector master poses + 6 Lottie/Rive animation sequences, rigging-ready) at ~$5k with an artist engaged, an AI-generated concept drawing exists as the foundation, and the **validation-first strategy** governs the spend: ship a working MVP **without** the mascot art or final visual polish, run a closed beta to test buy-in, and reinvest initial profits into the professional mascot animation, an outsourced professional UI/UX pass, and a marketing campaign for the official launch. | Owner decision 2026-09-01. Duolingo/Finch demonstrate mascot-driven retention; the fixed-visuals + prompt-driven-personality design makes the dynamic personality nearly free to build (no per-personality animation). Maps onto existing stages: Stages 4-6 build the companion feature set in plain UI, Stage 7 closed beta validates demand, reinvested profits fund the final-version art, UI/UX, and marketing at Stage 8 launch. The $5k art commitment is deferred until beta evidence justifies it. |
| D-037 | 2026-08-22 | Accepted | The bookcase **scrolls as one piece of furniture**: the crown molding and the leather bookmark ribbon were absolutely pinned over the shelf list, so scrolling to lower shelves left the case "top" floating mid-air over the books - the molding now renders as the shelf list's scrolling header, so crown and ribbon slide away with the top shelf exactly like a real case moving past the viewport. The ribbon keeps its tap target (QR bookmarks), entrance nudge, and drape over the molding; the dark back panel and side shades still fill the case interior behind the shelves. | Owner: "The top part of the bookshelf stays fixed in position after scrolling down... it looks weird" - a pinned molding breaks the physical metaphor the whole shelf design is built on. Scroll-away list headers are the platform-standard pattern. Shipped 2026-08-22 as an OTA update. |
| D-036 | 2026-08-22 | Accepted | Stage 3 gap-closure round, finishing the journey-map action items that remained buildable: (1) **first-run welcome** (J2) - the empty shelf now greets a new reader with a warm serif welcome, the one-line promise ("Your reading, in your own words"), a single gold **Add your first book** action, and a judgment-free hint that one sentence per sitting is plenty - progressive invitation instead of a tutorial, per the Stage 3 foundation; (2) **plain-language auth errors** (J1) - raw Supabase phrasing ("Invalid login credentials", "Email not confirmed", rate limits, network failures) is translated by a unit-tested mapper into human copy on sign-in, sign-up, and forgot-password; unknown errors pass through, the screen fallback covers the rest. The round's audit also confirmed three journey gaps were already met by earlier work: J3 QR states (registered/unclaimed/unlinked/conflict all have plain-language screens), J7 name-only character save, and J10 report confirmation + status tracking. | Owner: "finish the action items for stage 3." J2 and J1 were the last unbuilt Stage 3 journey items that don't depend on Stage 4/5 scope (J8/J11 companion and subscription surfaces are design-complete in DESIGN_REQUIREMENTS §4 and build against Stage 4 billing; J3's full payoff arrives with Stage 5 smart links; J10 illustrations wait on the brand pass). First-run teaching-by-invitation is the pattern in Bookly/StoryGraph onboarding; plain-language errors are NN/g error-message guidance. Shipped 2026-08-22 as an OTA update. |
| D-035 | 2026-08-22 | Accepted | The app gains a proper **Settings screen** (gear icon in the shelf header), completing the J9 relocation postponed from D-031: an iOS-Settings-style grouped list with the signed-in account (email + sync note), a **Your QR bookmarks** shortcut, **Report an issue**, and the app version - with **Sign out** as a destructive-styled action at the bottom behind a confirm dialog. The shelf header drops the bare "Sign out" text link (a destructive action next to everyday navigation, one accidental tap from logging out) and the floating "Something broken? Report an issue" link leaves the shelf for Settings. A leftover duplicate Author field on the add-book form (a D-032 edit slip) is removed. | Owner: "the settings window is not placed... make sure the app looks professional and polished." Every shipped reading app (Kindle, Apple Books, Goodreads, Bookly) keeps account/support behind a profile or gear entry, never on the home surface; destructive actions belong behind an intentional step (Apple HIG). Shipped 2026-08-22 as an OTA update. |
| D-034 | 2026-08-22 | Accepted | Searching past entries now **highlights every match inside the entry text**: the search on a book's Entries tab already filtered the list, but gave no cue *where* the word appeared; matches in the entry body and the page/chapter chip now render with a gold marker and bold weight, case-insensitively, for every occurrence. Implemented as a pure segment-splitting helper (unit-tested) so stored text is never modified - display-only, edit mode untouched. | Owner: "if I search for the word monk... I do not see where in the text it is being used." Highlighted hits are the universal search convention (browsers, editors, mail, Day One) - the eye lands on the match instead of re-reading the whole entry. Shipped 2026-08-22 as an OTA update. |
| D-033 | 2026-08-22 | Accepted | Picking a cover from the **cover search now also fills in blank book details**: the chosen match's author and median page count (Open Library `number_of_pages_median`) are applied to any field the reader left empty - typed input always wins (the standing scan contract), nothing is overwritten, and a toast with a one-tap **Undo** restores the prior blanks. Page counts vary by edition, so the toast says so whenever pages are filled, and every field stays editable. | Owner asked whether the cover picker knows the author and pages, then chose silent fill-blanks + Undo over an ask-first dialog. Fewer keystrokes lift form completion (Baymard); Undo-toast is the Material snackbar pattern for reversible automatic actions. The median-pages caveat is surfaced in the toast copy per the owner's edition-variance concern. Shipped 2026-08-22 as an OTA update. |
| D-032 | 2026-08-22 | Accepted | Owner review of D-031 on device, four refinements: (1) **two books per shelf restored** - three-across read as too small; at true 2:3 full-bleed, two-across is generous without the old toy-like feel (the typography ladder returns to its 2-across values); (2) **no color halo around real covers** - the painted cloth color still filled the tile behind full-bleed art and bled out as a faint tint at the rounded edges; under artwork the background is now transparent, and the finished gold ring is gone from real covers too (the corner medal is the whole celebration); (3) **the painted fallback recedes**: a soft ~14% ink veil sits over books without cover art (e.g. Monsterholic) so a placeholder never outshines real covers - finished painted books skip the veil (celebration is never dimmed); (4) **book details go minimal**: publisher and publication year are removed from add, edit, and the book header - a scan records title, author, total pages, and cover; Open Library still auto-fills missing pages; older books keep stored publisher/year in the database untouched, it is just no longer asked for or shown. | Owner: "three seem kind of small", "very faint color that surrounds the edge", "Monsterholic should not stand out", "publisher and publication year not necessary - scan should record title, author, total pages". Form-length research (Baymard, every removed field lifts completion) and competitor add flows (Goodreads/Bookly scan asks for nothing else) back the minimal record. Shipped 2026-08-22 as an OTA update. |
| D-031 | 2026-08-22 | Accepted | The **professional shelf redesign** round, opening the owner's "everything is open to redesign" mandate: (1) the shelf tightens from two to **three books across** - the density Kindle, Apple Books, and Bookly ship; two-across rendered covers ~240dp tall and toy-like; (2) real cover art renders **full-bleed** - the painted spine/hinge/page strips no longer overlay photographs (they squeezed art to ~0.59 of its width, the "covers look odd" report); painted cloth covers keep the full physical treatment; (3) a **Continue Reading hero card** above the bookcase replaces the spotlight halo/bubble/nudge: cover thumb, title, author, progress bar + %, position and last-entry line, one tap to resume - the Kindle/Bookly/StoryGraph home pattern; the hero book also keeps its shelf slot so the collection stays complete; (4) the case deepens to a **dark walnut** so cover colors pop forward (pale honey read as clip-art; dark interiors are how physical cases and Kindle/StoryGraph themes stage art), with a "X reading · Y finished" stat line; (5) **in-progress real covers** carry a slim dark scrim bar with gold fill + white % (the floating pill vanished on light art - the owner's Brothers Karamazov report); (6) **finished real covers** trade the gilt frame + plaque for a single gold corner **trophy medal** (Kindle's finished-badge pattern) - celebration without covering the art; (7) the empty-slot **resting pile retires** - painted filler read as fake beside real covers; (8) **add-book goes scan-first**: the typed-ISBN row is removed per owner, the scan card shows the lookup spinner, and manual fields remain the fallback (scan card still hides on builds without the camera). | Owner: "the current UI does not feel professional", "books lost their right ratio", "remove the ISBN row and just ask for the book scan", Brothers Karamazov % "not easy to see". All pure-JS, shipped 2026-08-22 as an OTA update - no reinstall needed. Postponed to keep the round shippable: settings/sign-out relocation to a home menu (J9), onboarding polish (J2) - logged in STAGE_3_DESIGN_FOUNDATION. |
| D-030 | 2026-08-22 | Accepted | Bookmarkt gains a **crash flight recorder**, and it caught its first crash: (1) every unhandled JS error is reported to the existing `analytics_events` table as an `app_error` event (message, stack, fatal flag, platform, running OTA update id) via a wrapped global error handler - fatal errors get up to 2 s to reach the server before the app closes; (2) the root layout exports a **screen error boundary** so render errors show a friendly "Something went wrong - Try again" card instead of killing the app; (3) the recorder immediately identified the standalone add-book crash the owner reproduced: `Cannot find native module 'ExpoCamera'` thrown while the add-book screen asked whether the scanner is available - the installed binary predates the camera module, and an OTA update delivered scanner JS to it; the module loader reports this error as fatal directly to the global handler, which is why neither the existing try/catch nor the new boundary could contain it; (4) the scanner guard is hardened to probe the native registry with `requireOptionalNativeModule('ExpoCamera')` (returns null instead of throwing) and only load expo-camera when the native side truly exists - builds without the camera hide the scan card gracefully, exactly as D-028 intended. | The crash was diagnosed remotely from the database in minutes instead of guessing from code review - the recorder is now standing infrastructure for every future report. Standing lesson recorded: an OTA update must never assume native modules that older installed binaries lack; guards must use the optional-module probe, and the runtime-version policy should move from a fixed string to Expo's fingerprint policy at the next native build (Stage 5 roadmap) so mismatched updates can never be delivered again. Owner reinstalls the current build to regain the scanner on the standalone app. |
| D-029 | 2026-08-22 | Accepted | First-look fixes on the real-cover shelf, from owner review of D-028 on device: (1) covers render at **true book proportions** - the cloth is now a 2:3 aspect-ratio box (the ratio Goodreads/Bookly/StoryGraph render covers at) instead of a fixed 176px height, so real cover art crops minimally instead of zooming into a detail; heights still derive from the shared slot width, so uniformity (D-026/D-027) holds; (2) **single-word titles always render on one line** - React Native breaks any overflowing word mid-word ("Monsterholic" became "Monsterhol / ic"), so the size ladder tightens (longest word ≤9 chars full size, ≤13 medium, else small) and one-word titles shrink first and ellipsize last; (3) the **edit screen's cover picker gains the exact-edition path**: type or scan the ISBN to fetch that printing's own cover (same guarded scanner; the resolved ISBN is stored on the book); (4) **finished treatment over real covers is redesigned to feel rewarding**: the diagonal FINISHED band (which landed across the cover art's title) is replaced by a stamped gilt inner frame plus a solid-gold trophy plaque along the base - celebration that never hides the artwork's identity; painted covers keep the original band + tooling. | Owner: covers "do not look scaled", the title fix "still doesn't reflect the improvements", the cover feature deserves ISBN/scan for existing books, and finished-with-cover "doesn't feel rewarding as it previously did". All four shipped 2026-08-22 as an OTA update; the scanner itself still arrives with the new native build. |
| D-028 | 2026-08-22 | Accepted | Real cover art and frictionless book entry ship together, both powered by **Open Library**: (1) books gain `cover_url` and `isbn` fields; (2) the add-book screen leads with **Scan the barcode** (EAN-13 via expo-camera, guarded exactly like voice per D-016 so builds without the module simply hide it) plus a typed-ISBN "Look up" fallback - one scan or lookup fills title, author, publisher, year, pages, AND the cover in a single request, with manual input always winning (the standing metadata contract); (3) add/edit forms offer a **cover picker** (search by title/author, choose from up to 8 candidates, or keep none) with the painted-cloth cover as the permanent fallback - shelf and book header render the real cover when set (finished books keep the gold border, FINISHED band, and gilt pages over real art; the tooling frame stays painted-only); (4) **legal basis**: Open Library explicitly supports moderate real-time human-facing lookups - fetches are per reader action, device-side, cached, never bulk, with "Covers from Open Library" attribution in the forms; **Google Books was declined** because its ToS forbids charging users for API-derived features without written permission, which conflicts with the paid companion; (5) scanned/typed ISBNs are checksum-validated (ISBN-10 and ISBN-13) so a misread barcode never fires a bogus lookup. Barcode-scan-to-add is the marquee frictionless-entry pattern in Goodreads and Bookly; real covers were D-027's top professional-finish gap. | Covers + ISBN lookup shipped 2026-08-22 (migration, forms, shelf, tests). Typed ISBN and cover search work on the current build via OTA; the camera scanner requires the next EAS build (native module) and hides itself until then. |
| D-027 | 2026-08-22 | Accepted | Professional-polish round against shipped competitors, plus three owner-reported fixes: (1) **all emoji UI chrome is replaced with vector icons** (Ionicons via @expo/vector-icons) - Apple HIG and Material both treat emoji-as-icon as unprofessional (OS-dependent rendering, unstylable, ambiguous to screen readers); (2) shelf covers get a truly **fixed uniform size**: every slot reserves the same bubble band and every cover is a fixed height, fixing the top-right book rendering taller than its neighbors (the spotlight's in-flow bubble had been shrinking its own cover while neighbors stretched); (3) cover **typography now steps down on total title length as well as longest word** (≤16 chars full size, ≤26 medium, longer small; the stricter of the two rules wins) - the old longest-word-only rule visibly never fired for normal multi-word titles; (4) the **unmark-finished rendering glitch** (cover painted as one solid color until re-finished) is fixed by keeping a constant transparent border on the elevated, clipped cover view instead of toggling border width - a known Android redraw pitfall; (5) the audit verdict: the warm-library concept differentiates, but finish quality trails until **real cover art** ships - recorded as the top proposed polish item (Open Library Covers API / Google Books thumbnails, needs schema field and owner approval); app icon/splash land in Stage 5 and empty-state illustration in J10. Owner's **AI capture assist** idea (voice describes a character; AI structures it into the four-field entry for reader review before save) is recorded as a Proposed paid-companion capability - it needs an explicit decision because D-012 says the companion never writes the record and D-016 limits cleanup to punctuation/casing; free capture stays frictionless regardless. | Icons, uniform covers, typography, and the unfinish fix shipped 2026-08-22 as an OTA update. Cover art and AI capture assist await owner approval before any build work. |
| D-026 | 2026-08-22 | Accepted | Book-screen and shelf refinements, each citing a proven pattern: (1) all shelf books share one **uniform size** - reading trackers (Bookly, StoryGraph, Kindle home) keep covers uniform and mark the active read with placement, badges, and motion, so the spotlight book keeps the lead slot, gold halo, ink-and-gold bubble, and peek nudge but no longer grows; (2) **Edit moves to the navigation bar** (platform convention for rare record-editing) and finishing becomes one prominent gold **status pill** directly under the title block - Goodreads/StoryGraph place reading status under the title and Bookly uses a bold colored finish button; (3) the capture bar is **context-aware** per Material FAB-per-context guidance: Entries = Write/Speak an entry, Characters = Add/Speak a character (the four-field form now opens on demand and dictation lands in the notes field for review), Photos = Add photos opening the picker; (4) high-volume journals stay scannable the way Day One/Journey/Apple Journal do it: the entries list groups under **day headings** (Today, Yesterday, weekday + date), an **entry search** appears from six entries, and the character form's progressive disclosure keeps the characters tab list-first with its search always available. | Owner feedback 2026-08-22: the hero-sized book made neighbors look thin (size prominence retired in favor of position + glow); "I finished this book" and "Edit book details" were lost in the text; the capture bar ignored the active tab; asked for the least-overwhelming way to store many entries. Capture stays two taps everywhere; free capture untouched. |
| D-025 | 2026-08-22 | Accepted | Finished books render as a matched **leather-bound collector set**: one shared deep-leather cover, gold-stamped title and author, gold tooling frame, gilt page edges, and a gold FINISHED band with ink lettering (band contrast fix). Dimming or graying finished books is explicitly rejected. A living **design requirements log** (docs/DESIGN_REQUIREMENTS.md) now records every look-and-feel requirement - experience pillars, per-surface requirements, research backing, and status - updated each design round. | Owner asked whether finished books should share a color scheme and darken like visited links. Research: dimmed treatment is the platform convention for disabled controls and would demote the product's proudest moment; Gestalt similarity says a shared treatment groups the trophy shelf; reading apps celebrate completion (Kindle badges, Apple Books collections). The collector-set metaphor is darker AND celebratory. Owner selected it from three options. The requirements log makes the accumulated design intent auditable. |
| D-024 | 2026-08-22 | Accepted | Research-validated refinements to the D-023 shelf, with the standing rule that design decisions cite published UX research or dominant patterns in shipped reading apps: (1) "Add a book" moves from a top button to a bottom-right floating + button - thumb-zone research (Hoober: ~49% one-handed use; bottom-right is the natural reach zone; top is the hard zone) and Material Design FAB guidance, freeing the top of the screen for the shelf; (2) the QR ribbon is recolored burgundy leather with a gold thread and hangs over a new crown molding so it never overlaps covers; (3) cover titles follow generated-cover typography (Kindle/Apple Books): wrap at spaces only, step type down within readable bounds for long words (NN/g: don't shrink below glanceable sizes), and ellipsize a single extreme word on one line - words never break mid-word; (4) the case gains depth: crown molding, plank-seam back panel, shaded sides, full-bleed shelf boards with under-shelf shadow, and a resting book pile in odd empty slots; (5) the spotlight book is the largest on the shelf (widest slot, tallest cover), its "last entry" bubble is ink-dark with gold text for contrast against the wood, and it peeks off the shelf once on arrival (one-time nudge, staggered before the ribbon's, never looping). | Owner direction 2026-08-22: ribbon color and overlap, add-book placement question, mid-word title breaks, "shelf looks plain/soulless", spotlight book must stand out most, gold-on-wood bubble lacked contrast. Size is the strongest prominence cue, so the hero book gets width and height, not just glow. Recording the evidence keeps future iterations honest. |
| D-023 | 2026-08-22 | Accepted | The bookshelf is redesigned around research-backed patterns from leading reading apps: books render as 2.5D covers (cloth color, paper label, page block) with a completion percent when page position and total pages are known; shelf order is recency-based - the most recently touched book takes the top-left slot with a gold spotlight and "last entry" bubble, untouched books follow by newest-added, finished books settle onto the lower shelves; tapping a book plays a pull-out animation before opening it. Readers can mark a book **finished** (`topics.finished_at`, additive migration): finished books show a gold treatment on the shelf, and finishing fires a `book_finished` analytics event - the roadmap's primary metric, now measured directly. QR-bookmark management moves from a top-level button to a ribbon bookmark draped over the bookcase frame (one-time nudge animation). | Recency sort and progress-on-cover are proven defaults across Apple Books, Kindle, Bookly, and StoryGraph; the shelf metaphor is Bookmarkt's locked identity (2026-08-21), so it is modernized rather than replaced. Finishing a book is celebrated (visible accomplishment, no streaks or guilt) and gives the primary metric a direct signal. The bookmarks button was misread as redundant chrome; the ribbon keeps the entry point discoverable inside the metaphor. |
| D-022 | 2026-08-22 | Accepted | The free re-entry panel prototyped in Stage 3 ("Where you left off" / "Pick up the thread") and the paid "Previously on..." recap merge into one paid companion feature, working name **"Where you left off"** (final name TBD). It displays only an AI-written recap of the reader's entries up to their latest one - a short story or bullet points, at a reader-chosen level of detail. No free verbatim-replay panel ships. Until the companion exists, the book screen shows a locked teaser row; taps are counted (ids and counts only) as a buying-interest signal. | A free panel that replays the reader's own notes adds too little beyond reading the notes (owner bar, 2026-08-22). The genuinely valuable version - AI synthesis of the story so far - is companion-grade and priced. The latest-entry boundary and free-capture-never-paywalled rules (D-012) are unchanged. Amends D-012's recap naming; narrows the J5 re-entry scope in the Stage 3 design foundation. |
| D-021 | 2026-08-22 | Accepted | The roadmap problem statement (§1) is expanded to name the critical-thinking dimension explicitly: P3 now states that tracking causality and connecting ideas across a long text is critical thinking in practice, and the consequence names the compounding erosion spiral. The honest coverage assessment records that this dimension is exercised chiefly by the paid companion (Socratic questioning, retrieval-practice cue cards and quizzes, continuity flags) as a mechanism-level benefit - part of the companion's price justification - never marketed as a measured outcome. | The paid companion's relevance is anchored in the problem statement rather than resting on feature appeal. Product claims remain bounded by the measurable metric (books finished); any future marketing language about thinking benefits must stay at mechanism level per this entry. Stage 3 companion-surface design (J8) should make the thinking value visible in the experience. |
| D-020 | 2026-08-21 | Accepted | Stage 2 (architecture rebuild) exit gate is closed with a product-owner `GO`. Evidence in [STAGE_2_EXIT_REVIEW.md](STAGE_2_EXIT_REVIEW.md): full retained-journey parity verified on the owner's device via the first EAS Android development build, voice capture verified end-to-end on device (D-016 flow), companion foundation passing grounding/entitlement tests, CI green on `main`, and the retirement runbook approved (D-019). iOS internal builds are formally deferred to Stage 5 (store packaging) pending an Apple developer account; device automation, the smart-link service (D-015), and structured telemetry remain tracked open items for later stages. | Stage 2 is complete; Stage 3 (Polished UI/UX) becomes the current stage. The iOS exit-gate criterion is carried to Stage 5 rather than waived silently. |
| D-019 | 2026-08-21 | Accepted | The PWA retirement runbook ([STAGE_2_OPERATIONS.md](STAGE_2_OPERATIONS.md) §7) is approved by the product owner in its hardened form: named freeze-breach approver with a mandatory cache-bump deploy checklist, retirement steps in execution order (cleanup service worker ships before QR repointing; the Auth redirect drop follows sunset-page verification), a deploy-preview test of the service-worker self-destruct, corrected session-expiry/RLS framing, a Netlify env-var audit, and a 14-day minimum notice window. | Exit-gate decision 3 is closed. The runbook executes in Stage 8 as written; material changes require a new decision entry. Review feedback that prompted the hardening is preserved in PR #47. |
| D-018 | 2026-08-21 | Accepted | The Stage 2 operations runbook in [STAGE_2_OPERATIONS.md](STAGE_2_OPERATIONS.md) is accepted: environment/build-profile mapping, the OTA-versus-native-build compatibility policy with one channel per environment, additive-migration deployment and forward-only rollback procedures, weekly Storage-object backups via `scripts/backup-storage.mjs`, native performance budgets, the internal release checklist, and the PWA freeze policy with its Stage 8 retirement runbook (routing swap, sunset page, service-worker self-destruct, cache cleanup, user communication, backend continuity). | Stage 2 releases and the frozen PWA are operated under these procedures. The retirement runbook's product-owner approval is recorded in D-019; material changes require a new decision entry. |
| D-017 | 2026-08-21 | Accepted | The Stage 2 architecture design in [STAGE_2_ARCHITECTURE.md](STAGE_2_ARCHITECTURE.md) is accepted: smart-link service with Universal/App Links and Android-only deferred deep links, twelve typed domain modules isolating Supabase, a server-authoritative companion entitlement boundary free capture never touches, the seven-point companion session contract, environment/secret/deployment ownership, and a single-repository layout with the Expo app under `app/`. | These constraints govern the Stage 2 scaffold and all native feature work. Material changes to any section require a new decision entry. |
| D-016 | 2026-08-21 | Accepted | Voice transcription runs on-device using the platform speech recognizers (iOS Speech framework, Android SpeechRecognizer) - no cloud transcription provider. Raw audio is kept only transiently on-device until the reader reviews and confirms the transcript, is never uploaded, and is deleted after confirmation or discard. The verbatim raw transcript is stored alongside the punctuation-and-casing-only cleaned text in the reader's entry rows, per the roadmap capture contract. If the OS recognizer is unavailable or requires network the app degrades gracefully to typed entry. | Zero per-minute transcription cost and no audio leaving the device (privacy principle). Accuracy is bounded by platform recognizers; revisit with a new decision if reader-confirmed accuracy proves inadequate. Transcription quality differences across devices become a Stage 3 UX-testing concern. |
| D-015 | 2026-08-21 | Accepted | Each physical bookmark carries a globally unique ID in its QR URL (still a Bookmarkt-controlled smart link per D-008). A bookmark is claimed into one account and linked to exactly one book at a time. Scanning a linked bookmark opens the app directly to that book's capture screen. The link is editable for the life of the bookmark: the owner can unlink it from a finished or dropped book and relink it to any other book in their library (Neuromancer today, Don Quixote tomorrow). Unlinked or unclaimed scans fall back to the claim/link flow after authentication; uninstalled scans follow D-008 store routing. | The data model gains a bookmarks domain: registry of manufactured IDs, claim, link, unlink, relink, and audit history. Manufacturing must print unique codes with batch tracking. In-app capture never requires a bookmark: scanning is an accelerator, and every capture feature remains reachable without one (free-capture principle). Cross-account transfer/resale and lost-bookmark replacement flows are deferred to a later decision; the ID registry designed here must not preclude them. |
| D-014 | 2026-08-21 | Accepted | The native application stack is React Native with Expo, TypeScript in strict mode, and the existing shared Supabase backend. Flutter and Capacitor were considered and declined: Flutter abandons the team's TypeScript/JavaScript experience and the PWA-derived logic; Capacitor wraps a web view rather than delivering the native UX the app-only launch scope requires. | Stage 2 scaffolds an Expo workspace with typed Supabase services. Expo EAS provides internal preview builds (Stage 2), native packaging and store submission (Stage 5). The PWA remains a frozen behavior reference only. |
| D-013 | 2026-08-21 | Accepted | Stage 1 exits early with a GO after five consecutive clean days (2026-08-16 to 2026-08-21) of daily product-owner use, waiving the final two days of the seven-day stability window defined in D-006. The observed window included the 2026-08-17 D-012 capture-first production deploy with zero defects. | Stage 1 is closed and Stage 2 (Architecture rebuild) opens 2026-08-21. Accepted risk: defects that would have surfaced on days 6-7 are undetected at exit; any post-exit P0/P1 is fixed with Stage 1 priority. Amends D-006 for this window only; the seven-day standard remains the default for future stability windows. |
| D-012 | 2026-08-17 | Accepted | Bookmarkt pivots from AI-generated content to reader-authored capture plus one paid AI Reading Companion. All reading records - summaries, notes, and character maps - are authored manually by the reader, typed or by voice. AI Summary, AI Character Mapping, and AI Image Generation are removed from the product; the image-generation backend stays dormant behind a server-side disabled flag. The Base/Base+/Ultimate ladder collapses to a single subscription: a companion that operates exclusively on the reader's own entries - Socratic dialogue, cue cards, "Previously on..." recaps, character-map quizzes, semantic search, cross-book threads, book-club prep, continuity flags, and a vocabulary bank. The reader's latest entry marks the content boundary. | The mission becomes recall/comprehension support for readers with fragmented attention, measured in books finished. The companion never writes the record and never presents model memory as unlabeled fact, structurally preventing the hallucination and spoiler failures observed in prototype AI generation. Free capture is never paywalled. Roadmap v2.0 redefines the Stage 2-8 work plans. Supersedes D-011. |

## D-008 scope clarification

- Stage 1 PWA evidence remains valid as historical prototype and backend-behavior
  evidence.
- The PWA may remain available during Stages 2-7 as a frozen internal/reference
  implementation and may receive critical stabilization fixes.
- New product capabilities target the native application rather than expanding
  the PWA into a permanent channel.
- The Bookmarkt-controlled HTTPS destination remains permanent because iOS
  Universal Links, Android App Links, platform store routing, and printed QR
  durability require it.
- Unsupported or desktop scans may show installation information, but no web
  reading application ships at v1.

## D-011 scope clarification

*Historical - superseded by D-012 on 2026-08-17. Retained because it defined the
server-authoritative entitlement rigor that D-012 carries forward to the
companion subscription.*

- Authentication and remaining quotas are necessary but not sufficient for AI
  access; the verified active paid tier must authorize the entire requested
  feature set.
- The feature ladder is cumulative and fixed:
  - Base: AI Summary.
  - Base+: AI Summary and AI Character Mapping.
  - Ultimate: AI Summary, AI Character Mapping, and AI Image Generation.
- Feature selection is set-based:
  - Base has one valid set: Summary.
  - Base+ has three valid non-empty sets: Summary; Character Mapping; both.
  - Ultimate has seven valid non-empty sets: each feature individually, each
    two-feature combination, and all three.
- "Together" means one user action and one generation session. Selected provider
  work runs concurrently where technically safe. Internal sequencing is permitted
  for a genuine dependency but must not require separate user workflows.
- Every selected feature uses the same immutable reading boundary and has its own
  amount/detail configuration, execution status, output, and approval state.
- Pricing, billing periods, optional introductory offers/trials, and per-feature
  quotas remain Stage 4 implementation decisions.
- Entitlement denial occurs before quota consumption and before an external AI
  provider is contacted.
- Store selection alone does not grant access. Purchase verification and
  server-authoritative entitlement activation are required.
- Declining, canceling, abandoning, or failing a purchase returns the reader to
  manual entry without deleting unsaved manual work.
- A client-side hidden button or paywall is user experience only, not the
  authorization boundary.
- Manual notes, manual character-map maintenance, and personal image uploads
  remain outside the paid-AI catalog unless a later material decision changes
  them.
- AI output is reviewable before save. Text and character artifacts use
  user-owned RLS rows; generated images use private user-scoped Storage.
- Entitlement and quota preflight covers the full selected feature set and is
  all-or-none before provider calls. After execution begins, successful outputs
  survive a sibling failure and only failed work may be retried idempotently.
- Adding another AI capability requires a material roadmap decision rather than
  silently adding it to an existing tier.

## D-012 scope clarification

Problem statement and mission:

- The problem is fragmented attention impairing book reading in three ways:
  P1 stamina (sustaining sessions), P2 recall (losing the thread between
  sessions, leading to abandonment), and P3 comprehension (tracking characters,
  plot, and causality).
- The mission: for individuals whose fragmented attention impedes their ability
  to read for long periods or to recall and understand what they're reading,
  Bookmarkt makes it easy to pick a book back up, stay oriented in it, and
  finish it. The success metric is books finished.
- The product addresses P2 directly, P3 through active production, and P1 only
  indirectly. This gap is recorded honestly in the roadmap; direct session
  mechanics remain an additive post-launch candidate.

Authoring and capture:

- The reader authors everything. Summaries, notes, and character maps are
  written by the reader - typed or by voice. No product surface writes the
  reading record.
- One-sentence entries are always acceptable and never judged or graded.
- Voice capture preserves the raw transcript; cleanup touches punctuation and
  casing, never meaning. The reader reviews and confirms the transcript.
- The reader's latest entry marks the upper content boundary for every
  companion feature.
- Free capture - entries, character maps, progress/boundary tracking, metadata,
  and personal images - is never paywalled.

The AI Reading Companion (single paid subscription):

- The companion operates exclusively on the requesting reader's own entries. It
  never recalls the book from model memory as unlabeled fact and asks more than
  it answers.
- Provenance ladder: answers ground in the reader's notes first; model
  knowledge appears only on explicit request and is labeled as such; on weak
  recognition the companion declines rather than guesses. It never grades a
  reader's answer using unverifiable knowledge.
- Socratic dialogue is ungraded by default. When a reader's answer conflicts
  with their own notes, the companion mirrors the notes back. Wrong answers are
  explored with follow-up questions rather than corrected outright; right
  answers are occasionally probed.
- Features: Socratic dialogue, cue cards with deterministic spaced repetition,
  "Previously on..." recaps, character-map quizzes (the reader's map is the
  answer key), semantic search, cross-book threads, boundary-bounded book-club
  prep, continuity flags between the reader's entries, and a vocabulary bank.
- The trial is server-authorized and begins only after a few entries exist.
- The backend validates identity, active companion entitlement, and usage quota
  before any provider call; denied requests consume nothing.

Removed and rejected scope:

- AI Summary, AI Character Mapping, and AI Image Generation are removed. The
  image-generation backend remains dormant behind a server-side disabled flag;
  re-enabling it requires a new material decision. The `book-images` Storage
  bucket is unchanged.
- Read-next recommendations: rejected as commodity.
- AI-generated character bios or world guides: rejected - this is the prototype
  failure mode that motivated the pivot.
- Note-quality coaching or grading: rejected - grading capture kills the habit.

Minors and compliance:

- "Individuals" brings minors into scope. Launch posture: 13+ with an age gate.
  Under-13 access requires COPPA/GDPR-K parental-consent work and a separate
  material decision before any expansion. App-store age ratings follow the 13+
  posture. This posture is revisitable.

Stage 1 disposition:

- Stage 1 observation continues unchanged against the retained scope:
  authentication, storage isolation, sync, manual entry, images, and backend
  cost controls.
- Prototype AI-generation evidence is retired as launch-feature evidence but
  remains valid for the backend controls it exercised. The deployed prototype
  AI endpoints keep their Stage 1 safeguards until removed by a code change.

## Decision status

- **Proposed:** Under review and not authoritative.
- **Accepted:** Active and reflected in the roadmap.
- **Superseded:** Replaced by a later decision that cites the original ID.
- **Rejected:** Considered but not adopted.

## Entry template

Add material decisions as a new row and include supporting detail below when the
tradeoff is not self-evident.

```text
ID:
Date:
Status:
Context:
Decision:
Alternatives considered:
Affected stages:
Consequences and risks:
Approver:
Supersedes:
```
